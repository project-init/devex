numLookBackDays: 90
reposToSkip:
  - "documentation"
  - "super-secret-repo"

# Only enable this if you want to get specific repos. Otherwise, the tool will pick up all repos in your organization.

# reposToCheck:
#   - "onlyRepoICareAbout"

outputDirectories:
  prs: "cmd/devex/contributions/prs"
  signals: "cmd/devex/contributions/signals"

organization: project-init
