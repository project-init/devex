# Discovery TODO

- [ ] Support `default_target` in `.sre/discovery.yaml`. Resolve publication targets in this order: an explicit `--target`, the configured `default_target`, the only configured target, or an actionable error listing available targets when multiple targets remain ambiguous.
