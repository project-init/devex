dependencies:
  upgrade: [mise, go]
  go:
    # go.mod holds the minor line's floor (1.27.0); mise pins the patch.
    target: patch
    directive: minor
  mise:
    policies:
      # golangci-lint majors change its config format, so they move by hand.
      golangci-lint: minor
